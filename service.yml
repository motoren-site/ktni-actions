kind: Service
apiVersion: v1
metadata:
  name: ktni-actions
  namespace: apps
  k8s-app: ktni-actions
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  selector: ktni-actions
  type: ClusterIP